#include "Tracking.h"  // 包含循迹相关的头文件
#include "Delay.h"    // 包含延时相关的头文件

// 八路循迹函数
void Tracking_8Sensors(void) {
    // 1号传感器（最左边）检测到黑线，其余未检测到
    if(D1==1 && D2==0 && D3==0 && D4==0 && D5==0 && D6==0 && D7==0 && D8==0) {
        motor(-30,30);  // 小车向左急转，尝试使黑线进入中间传感器
    }
    // 8号传感器（最右边）检测到黑线，其余未检测到
    else if(D1==0 && D2==0 && D3==0 && D4==0 && D5==0 && D6==0 && D7==0 && D8==1) {
        motor(30,-30);  // 小车向右急转，尝试使黑线进入中间传感器
    }
    // 1号和2号传感器检测到黑线，其余未检测到
    else if(D1==1 && D2==1 && D3==0 && D4==0 && D5==0 && D6==0 && D7==0 && D8==0) {
        motor(0,30);  // 小车向右调整，使黑线居中
    }
    // 7号和8号传感器检测到黑线，其余未检测到
    else if(D1==0 && D2==0 && D3==0 && D4==0 && D5==0 && D6==0 && D7==1 && D8==1) {
        motor(30,0);  // 小车向左调整，使黑线居中
    }
    // 2号传感器检测到黑线，其余未检测到
    else if(D1==0 && D2==1 && D3==0 && D4==0 && D5==0 && D6==0 && D7==0 && D8==0) {
        motor(20,50);  // 小车向右微调
    }
    // 7号传感器检测到黑线，其余未检测到
    else if(D1==0 && D2==0 && D3==0 && D4==0 && D5==0 && D6==0 && D7==1 && D8==0) {
        motor(50,20);  // 小车向左微调
    }
    // 3号传感器检测到黑线，其余未检测到
    else if(D1==0 && D2==0 && D3==1 && D4==0 && D5==0 && D6==0 && D7==0 && D8==0) {
        motor(10,60);  // 小车向右微调
    }
    // 6号传感器检测到黑线，其余未检测到
    else if(D1==0 && D2==0 && D3==0 && D4==0 && D5==0 && D6==1 && D7==0 && D8==0) {
        motor(60,10);  // 小车向左微调
    }
    // 4号传感器检测到黑线，其余未检测到
    else if(D1==0 && D2==0 && D3==0 && D4==1 && D5==0 && D6==0 && D7==0 && D8==0) {
        motor(20,50);  // 小车向右微调
    }
    // 5号传感器检测到黑线，其余未检测到
    else if(D1==0 && D2==0 && D3==0 && D4==0 && D5==1 && D6==0 && D7==0 && D8==0) {
        motor(50,20);  // 小车向左微调
    }
    // 所有传感器都检测到黑线
    else if(D1==1 && D2==1 && D3==1 && D4==1 && D5==1 && D6==1 && D7==1 && D8==1) {
        motor(50,50);  // 小车直行
    }
    // 其他情况
    else {
        motor(50,50);  // 小车直行或根据实际情况调整
    }
}

